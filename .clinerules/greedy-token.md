# greedy-token

Поиск в коде — CLI `greedy-token` или MCP `greedy_token_search`, не серия обходов всего репо.

Повторную проверку («есть ли поле», «не пропал path») — python-скрипт, не новый Task.

Не обещать, что чат Cline станет бесплатным: дешевле, когда повтор гоняет скрипт.
