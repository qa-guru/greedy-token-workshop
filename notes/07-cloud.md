# Шаг 7 — облако

[ollama.qa.guru](https://ollama.qa.guru) за nginx Basic. Без `OLLAMA_USER`/`OLLAMA_PASSWORD` проба `/api/tags` даёт 401. Это нормальный конец шага у студента, не ошибка занятия.

Не подставлять `localhost:11434` вместо облака. Локальная и облачная модели в yaml — разные `id`.
