# Шаг 3 — схема, не поиск

`rg email` / `greedy_token_search email` находит строки с адресами. Объект `{"id": 2}` в выдачу не попадает как «дыра».

Нужен jq `map(keys)` или скрипт шага 4. Если агент снова ищет слово `email` — шаг не засчитан.
